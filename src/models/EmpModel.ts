class EmpModel {
    badgeNo: string;
    firstName: string;
    lastName: string;

    constructor(
        badgeNo: string,
        firstName: string,
        lastName: string
    ) {
        this.badgeNo = badgeNo;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}