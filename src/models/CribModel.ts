class CribModel {
    code: string;
    description: string;

    constructor(
        code: string,
        description1: string, 
    ) {
        this.code = code;
        this.description = description1;        
    }

}